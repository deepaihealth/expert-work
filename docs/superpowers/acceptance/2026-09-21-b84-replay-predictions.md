# B-84 本波重放验收 —— 开跑前的预测（写死，事后不许改判据）

**写于 2026-09-21，在任何重放跑起来之前。** 目的:防止我事后给观测到的任何结果编一个解释。
（今天已经犯过一次:看到测试数 10,160 对不上就编了「venv 装了 docx」的理由,真相是我的树是旧的。）

## 被重放的 run

`7fad305b`（2026-09-16，用户 `92c5b903`，ai-health-plan，线程首轮，可重放）

历史事实（测于当时的测试环境 `621249f6`）:

| 项 | 值 |
|---|---|
| 墙钟 | 1,178s |
| LLM 调用 | 24 次 |
| 输入 token | 1,264,709 |
| 输出 token | 62,971 |
| `list_dir` | 3 次，其中 1 次 `sandbox create failed: 504` |
| 重打 `style/render_plan.py` | 20,064 B |
| 时间预算拆解 | 工具 123s(10.4%) + prefill 34s(2.9%) + decode 1,021s(86.7%) |

## ⚠️ 这次重放测的不只是本波

09-16 至今测试环境经过四次发布:

| tag | 内容 |
|---|---|
| `42426d31` (09-17) | B-61/B-67 注入变量按引用 + 本轮输入平台接管 |
| `233791e5` (09-19) | #1608 技能块砍 63~65%（已实测只省约 0.6s/轮） |
| `139057c8` (09-20) | B-85 ③ |
| `927087ed` (09-21) | **本波** |

**所以「改进了多少」的答案里有别人的功劳。** 要单独摘出本波，必须在 `139057c8` 上再跑一臂。

## 预测（可证伪，按重要性排）

| # | 预测 | 若不成立怎么读 |
|---|---|---|
| 1 | **不再重打 `render_plan.py`**（0 B） | 仍然重打 → 本波对这个场景无效，工作区块没起作用 |
| 2 | **`list_dir` 失败 0 次** | 仍失败 → PR-2b 的宿主 NAS 读没生效，去查部署 |
| 3 | **墙钟落在 850~1,100s** | **低于 600s → 有我没算到的因素，去查，不要庆祝**；高于 1,178s → 本波是净负 |
| 4 | **输出 token 55,000~60,000** | 省掉的约 5,000 token 正是那 20,064 字符的重打 |
| 5 | **输入 token 上升 1~2%** | 工作区块每轮约 725 token × 24 轮 ≈ 17,400。**这条是对本波不利的预测，特意写进来** |

## 量级预估的算术（先摆出来，免得事后倒推）

省掉 20,064 字符 ≈ 5,016 输出 token × 18 ms/token ≈ **90 秒**，占 1,178s 的 **7.6%**。

**所以本波在这个 run 上的理论上限就是省 7~8%，不是「快一倍」。** 任何超出这个量级的改进都来自别处，必须归因清楚。

## n=1 的边界

模型轨迹每次都变，单次结果不能当发生率。因此:
- 新环境上**跑 3 次**取散布，不是 1 次
- 发生率（5% 那个数）只能靠群体统计泡一周，本次重放答不了
