# 第三方对接 API v1 —— P3 波 2 实施计划(帧文档补全 + 四语言示例)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to
> implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 SSE 流里已经存在、但文档里没有的东西写清楚 —— 重点是 `updates` 帧怎么解析
(第三方在自己界面还原 agent 交互过程的唯一依据),外加 `worker`/`guard`/`compaction` 三帧补录、
图片张数两道闸的勘误,以及第 8 章四语言示例。

**Architecture:** 纯文档 + 侧栏配置。**不动任何后端代码。** 两个 PR 并行、各自 worktree:

| PR | 范围 | 文件 |
|---|---|---|
| **PR-2** | 第 5 章重构(5.1–5.6)+ `updates` 详解 + 三帧补录 + 图片闸勘误 | `guide/sse-events.md`、`guide/run-agent.md`、`.vitepress/config.mts` |
| **PR-3b** | 第 8 章四语言示例 | `guide/examples.md`(新建)、`.vitepress/config.mts` |

**Tech Stack:** VitePress(默认主题,`::: code-group` 原生 tab)、Markdown。示例语言:
curl / Node.js(v22,内建 fetch)/ Python 3(标准库 `urllib`,**不引第三方包**)/ Java **JDK 8 +
`HttpURLConnection`**(本机 `java 1.8.0_331` / `javac 1.8.0_302`,示例必须能在这套上编译运行)。

## Global Constraints

- **唯一事实来源是 `.superpowers/sdd/2026-08-15-p3-wave2/contract-frames.md`**(2026-08-15 真栈抓取)。
  与它冲突的旧文档、spec 措辞一律以它为准;它没覆盖到的字段**不要编**,宁可不写。
  原始帧在同目录 `updates_frames.json`,要更多样例直接读。
- **机密红线**(沿用 #1151):公开文档不得出现凭据、密钥名、金库路径、内网地址、集群串、
  内部服务名、**内部模块路径**。契约表里出现的 `sse.py:509` / `spotlight.py` 这类定位信息
  **只供你核对,绝不能写进文档**。
- 域名一律写 `https://<your-domain>`(与现有 8 篇一致)。
- 全中文;章节编号手写(VitePress 不自动编号);侧栏链接的锚点必须真实存在。
- **只改本 PR 名下的文件。** 两个 PR 都要动 `.vitepress/config.mts`,各自只碰自己那一块
  (PR-2 = 第 5 章的 `items`;PR-3b = 第 8 章那一个条目),别顺手重排别人的。
- 不新增依赖、不装插件、不改 CI。
- 文风对齐已有 8 篇:先说结论、坑用 `::: warning` 块、表格给字段、句末不留空格
  (pre-commit 会卡尾随空白)。

---

# PR-2:第 5 章重构 + 帧文档补全 + 图片闸勘误

分支 `docs/p3-sse-frames`。

### Task 1: 第 5 章骨架重排 + 事件总表补三帧

**Files:**
- Modify: `apps/admin-ui/docs-site/guide/sse-events.md`
- Modify: `apps/admin-ui/docs-site/.vitepress/config.mts`

**Interfaces:**
- Produces:第 5 章的六个二级标题(Task 2/3 往里填内容),锚点形如
  `#_5-3-updates-帧怎么解析`(VitePress 中文锚点规则:数字开头会加 `_` 前缀,空格→`-`)。

- [ ] **Step 1: 把现有内容归位到 5.1–5.6**

现有 `sse-events.md` 已有的内容按 spec §七 的编号重排,**内容不要重写**,只做归位与补标题:

```
## 5.1 帧格式          ← 现「流从哪来」+「帧长什么样」
## 5.2 事件总表        ← 现「事件类型一览」
## 5.3 updates 帧怎么解析   ← 新增,Task 2 填
## 5.4 token 帧        ← 现「token 事件:仅供预览,不是最终结果」
## 5.5 worker / guard / compaction 帧  ← 新增,Task 3 填
## 5.6 断线重连        ← 现「断线重连」整节(含 gap / 回放分页 / 游标)
```

`end` 帧那一节(现「`end` 事件:流怎么结束的」)并进 5.2 之后作为 5.2 的尾部小节,
标题降为 `### end 帧的终局状态`——它是事件总表的一部分,不值一个二级号。

- [ ] **Step 2: 事件总表补三行**

在 5.2 的表格里,`token` 行之后按 contract-frames.md §1 补三行(**位置放在 `token` 之后、
`gap` 之前**,因为这三个是可回放的落库帧,和上半张表同性质):

| `event:` | 什么时候出现 | 有 `id:` | 断线重连能拿回来吗 |
|---|---|---|---|
| `worker` | Agent 委托子任务(worker)时,子任务的开始 / 每步 / 结束各发一次 | 有 | 能 |
| `guard` | 平台护栏触发或预警(步数上限 / token 预算 / 无进展) | 有 | 能 |
| `compaction` | 上下文过长被自动压缩时发一次 | 有 | 能 |

同时把 5.2 现有那句"这张表列的是当前主要会遇到的事件类型,不是穷举"保留 —— 它现在更准了。

- [ ] **Step 3: 侧栏加第 5 章二级条目**

`.vitepress/config.mts` 里 `{ text: "5 SSE 事件格式", link: "/guide/sse-events" }` 那一项改成带
`items` 的形式,六个子项照 Step 1 的标题与锚点写。**格式对齐第 2/3/7 章已有的写法。**

- [ ] **Step 4: 验证 —— 构建 + 锚点真实存在**

```bash
cd apps/admin-ui/docs-site && pnpm install --frozen-lockfile && pnpm build
```
Expected: 构建成功。然后逐条核对侧栏锚点确实在产物里:

```bash
python3 - <<'PY'
import re, pathlib
cfg = pathlib.Path("apps/admin-ui/docs-site/.vitepress/config.mts").read_text()
html = pathlib.Path("apps/admin-ui/docs-site/.vitepress/dist/guide/sse-events.html").read_text()
anchors = re.findall(r'link: "/guide/sse-events#([^"]+)"', cfg)
missing = [a for a in anchors if f'id="{a}"' not in html]
print("侧栏锚点", len(anchors), "缺失", missing)
assert anchors and not missing
PY
```
Expected: `缺失 []`(**注意:必须先跑通"故意写错一个锚点 → 这段脚本报错"再改回来**,
否则你不知道它是不是恒绿的)。

- [ ] **Step 5: Commit**

```bash
git add apps/admin-ui/docs-site
git commit -m "docs(external-api): SSE 第 5 章按 5.1-5.6 重排,事件总表补 worker/guard/compaction"
```

### Task 2: 5.3 `updates` 帧怎么解析(本 PR 的核心)

**Files:**
- Modify: `apps/admin-ui/docs-site/guide/sse-events.md`(只填 5.3 一节)

**Interfaces:**
- Consumes:Task 1 建好的 `## 5.3 updates 帧怎么解析` 空标题。

- [ ] **Step 1: 写 5.3,按下面的顺序**(事实全部来自 contract-frames.md §2,一条都不要多编)

1. **一句话定位**:`updates` 是这一步的**权威结果**;界面该用它重建,`token` 只是预览。
2. **形状**:`data` 是 `{节点名: 节点写入}`,一帧通常只有一个节点键。给一个真实样例
   (从 `updates_frames.json` 的 B 场景取,**把 `nonce` 之类的随机串换成占位**)。
3. **`::: warning` 块 —— 节点写入可能是 `null`**。给 `{"workspace_ingest": null}` 的真实样例,
   明说"先判 null,再取 `messages`";这是最容易在对接第一天就崩掉的地方。
4. **节点名不是固定枚举**:列出真栈见过的 `memory_recall` / `workspace_ingest` / `agent` /
   `tools` / `memory_writeback`,说明还有按 Agent 配置才出现的(不必列全),
   并给出规则:**遇到不认识的节点名,忽略这一帧即可,不要报错**。
5. **只有三个字段需要读**:`messages`、`step_count`、`_duration_ms` —— 各给一句话含义。
   其余通道明确标成"内部调度字段,不保证稳定,别依赖"(把名字列出来,免得对接方以为是自己漏了)。
6. **`messages[]` 两种形状**,各给一张字段表 + 一个真实样例(截断):
   - `type: "ai"`:`content`(**空串是正常的** —— 只发工具调用那一步就是空)、
     `tool_calls[]`(`{name, args, id}`)、`response_metadata.finish_reason`
     (`tool_calls` / `stop`)、`usage_metadata`(可直接做用量展示)、
     `additional_kwargs.reasoning_content`(模型思维链,**不保证每个模型都有、也不保证长期存在**)。
   - `type: "tool"`:`name`、`tool_call_id`、`content`、`status`、`artifact`、
     `additional_kwargs.duration_ms`。
7. **配对键**:`ai.tool_calls[].id` ←→ `tool.tool_call_id`。**界面把"调用"和"结果"连起来就靠它** ——
   这条单独成段,别埋在表格里。
8. **`::: danger` 块 —— 工具结果的文本是防注入包装过的,直接渲染就是乱码**。
   照 contract-frames.md §2 最后一节写:围栏 + `▁` 数据标记,给出真实的乱码样例
   (`Wrote▁ 11▁ bytes▁ to▁ probe_note.txt`),给出还原三步(去开围栏 / 去闭围栏 / 删 `▁` 字形),
   并**如实说明还原是有损的:原文换行拿不回来**。最后给一句:围栏在不在本身是有用信号
   (这段内容来自外部、不可信),建议保留成一个标志位。
   **正则要给全**(对接方直接抄):`/«UNTRUSTED nonce=[^»]*»\n?/g`、`/\n?«\/UNTRUSTED nonce=[^»]*»/g`、`/▁/g`。
9. **帧量级**:给真栈实测的三组数(简单问答 4 条 `updates` / 58 条 `token`;工具调用 8 / 146;
   分两步 6 / 954),说明 `token` 占九成以上 —— 这是"别拿 token 重建状态"的实测依据。

- [ ] **Step 2: 自查 —— 文档里的每个字段名都能在原始帧里找到**

```bash
python3 - <<'PY'
import json, pathlib, re
doc = pathlib.Path("apps/admin-ui/docs-site/guide/sse-events.md").read_text()
raw = pathlib.Path(".superpowers/sdd/2026-08-15-p3-wave2/updates_frames.json").read_text()
sec = doc.split("## 5.3")[1].split("\n## ")[0]
# 文档里出现的 `xxx_yyy` 形态字段名,逐个回到原始帧里找
names = set(re.findall(r"`([a-z_]{4,})`", sec))
missing = sorted(n for n in names if n not in raw)
print("5.3 提到的字段:", len(names), "原始帧里找不到的:", missing)
PY
```
Expected: `找不到的` 只剩非字段词(如 `messages` 之外的散文词)。**逐个人工确认**:
剩下的每一个都必须是散文用词或 contract-frames.md 里有出处的,**不能有你自己编的字段名**。

- [ ] **Step 3: 构建**

Run: `cd apps/admin-ui/docs-site && pnpm build`
Expected: 成功。

- [ ] **Step 4: Commit**

```bash
git add apps/admin-ui/docs-site/guide/sse-events.md
git commit -m "docs(external-api): 5.3 updates 帧详解——节点/null/消息配对/防注入包装还原"
```

### Task 3: 5.5 三帧补录 + 图片张数两道闸勘误

**Files:**
- Modify: `apps/admin-ui/docs-site/guide/sse-events.md`(5.5 一节)
- Modify: `apps/admin-ui/docs-site/guide/run-agent.md:164` 附近

- [ ] **Step 1: 写 5.5**(事实来自 contract-frames.md §3)

三个小节,各给 `data` 字段表 + 一句"什么时候会看到":

- `worker`:先说清每帧都带的那组固定字段(`worker_id` / `parent_worker_id` / `parent_tool_call_id` / `label` /
  `agent_ref` / `depth` / `kind` / `wseq` / `data`),再按 `kind` 分 `start` / `update` / `end` 三张表。
  **两个必须写的坑**:① `wseq` 是这个 worker 自己的序号,**和 SSE 的 `seq` 无关,别拿它当游标**;
  ② `update` 里的 `messages` **是摘要不是原样消息**(字段名带 `_excerpt`,内容 500 字符 /
  参数 200 / 结果 500 截断),而且**同样没剥防注入包装**,渲染前照 5.3 的三步还原。
  `parent_tool_call_id` 指回父层那次工具调用 —— 界面把子任务挂到对应工具卡下面就靠它。
- `guard`:`{kind, guard, detail}`;`kind` 两值 `tripped` / `warning`;`guard` 三值
  `max_steps` / `token_budget` / `no_progress`,各自的 `detail` 形状给出来。
  说明这是**平台护栏**而不是错误:收到 `tripped` 意味着这一轮会被收尾,不是 run 崩了。
- `compaction`:`{passes, tokens_before, tokens_after, summary_chars}`;
  一句话:上下文太长被自动压缩,历史被摘要替换,**不影响这次回答的正确性,但界面可以提示用户**。

- [ ] **Step 2: 改 `run-agent.md` 的图片张数**

现在那句「两者合计仍然不能超过 64 张,超了是 422、`error.code` 为 `TOO_MANY_IMAGE_REFS`(统一信封,
不是裸 `detail`)」**是错的**(只描述了两道闸里的一道)。照 contract-frames.md §5 改成两档:

| 张数 | 响应 |
|---|---|
| 超过 64 张 | 422,**统一信封**,`error.code` 为 `TOO_MANY_IMAGE_REFS` |
| 9 ~ 64 张 | 422,**裸 `detail`**:`{"detail": "too many images: max 8 per run"}`,**没有 `error.code`** |

并明说:**实际能过的是平台配置的每次上限(默认 8 张)**,64 只是"合并列表长度"的硬上限;
两档错误形状不一样,解析错误时两种都要能认。
到 `errors.md` 的错误码总表核对一下 `TOO_MANY_IMAGE_REFS` 那一行是否也要补一句(有就补,没有别新增行)。

- [ ] **Step 3: 构建 + 全文再过一遍机密红线**

```bash
cd apps/admin-ui/docs-site && pnpm build
cd - && rg -n "sse\.py|builder\.py|spotlight|_worker_events|control_plane|orchestrator|127\.0\.0\.1|expert-work-test|\.svc|kubectl" apps/admin-ui/docs-site/guide/
```
Expected: 构建成功;第二条命令**零命中**(命中即违反机密红线,必须删)。

- [ ] **Step 4: Commit**

```bash
git add apps/admin-ui/docs-site
git commit -m "docs(external-api): 5.5 worker/guard/compaction 三帧补录 + 图片张数两道闸勘误"
```

---

# PR-3b:第 8 章四语言示例

分支 `docs/p3-examples`。

### Task 4: `examples.md` —— curl + Python

**Files:**
- Create: `apps/admin-ui/docs-site/guide/examples.md`
- Modify: `apps/admin-ui/docs-site/.vitepress/config.mts`(只加第 8 章一个条目)

**Interfaces:**
- Produces:`## 8.1 …` 起的小节骨架 + `::: code-group` 块,Task 5 往同样的 code-group 里加
  Node.js / Java 两个 tab。**code-group 里的 tab 顺序固定为 curl → Python → Node.js → Java。**

- [ ] **Step 1: 建页面骨架**

八个场景各一节(顺序照 spec §七):

```
# 8 附录:多语言示例
## 8.1 发起 run(stream)并解析 SSE
## 8.2 queue 模式 + 轮询结果
## 8.3 上传文件并带进 run
## 8.4 续接会话
## 8.5 断线重连(带 since_seq)
## 8.6 取消 run
## 8.7 审批决策
```

开头一段说明:所有示例把 key 从环境变量读(**示例代码里绝不写死 key**),
域名统一 `https://<your-domain>`,并指一句"字段含义见第 4 章、帧含义见第 5 章"。

- [ ] **Step 2: 8.1 的 curl + Python**

curl 用 `-N`(与 quickstart 一致)。Python 用**标准库**(`urllib.request` + 手写分帧),
必须正确处理这四件事,每件在代码注释里点出来:

1. 按空行(`\n\n`)分帧,**不是按行**;
2. 跳过以 `:` 开头的心跳注释行(服务端约 15 秒一个);
3. 从 `id:` 里取 `seq`(形如 `{毫秒}-{seq}`,取最后一个 `-` 之后的部分),
   维护"见过的最大 seq"当重连游标;
4. 收到 `end` 才算结束;收到 `truncated` **不算结束**(要带 `next_seq` 继续拉)。

- [ ] **Step 3: 8.2–8.7 的 curl + Python**

每节都给完整可跑的最小代码,不要 `...` 省略。`user_id` 的位置按第 4 章:
取消 / 审批在**请求体**,归档在 query —— 别弄反(这是真栈验收时踩过的坑)。

- [ ] **Step 4: 本地语法检查**

```bash
python3 - <<'PY'
import ast, pathlib, re
md = pathlib.Path("apps/admin-ui/docs-site/guide/examples.md").read_text()
blocks = re.findall(r"```python\n(.*?)```", md, re.S)
print("python 代码块:", len(blocks))
for i, b in enumerate(blocks):
    ast.parse(b)          # 语法错就在这里炸
print("全部 parse 通过")
PY
```
Expected: 块数 ≥ 7,全部通过。(`ast.parse` 绿不代表跑得通,真机跑在 Task 6。)

- [ ] **Step 5: 侧栏 + 构建 + Commit**

`.vitepress/config.mts` 加 `{ text: "8 附录:多语言示例", link: "/guide/examples" }`(放第 7 章之后)。

```bash
cd apps/admin-ui/docs-site && pnpm build
git add apps/admin-ui/docs-site
git commit -m "docs(external-api): 第 8 章多语言示例——curl + Python"
```

### Task 5: Node.js + Java 两个 tab

**Files:**
- Modify: `apps/admin-ui/docs-site/guide/examples.md`

- [ ] **Step 1: Node.js(v22,内建 fetch,零依赖)**

用 `fetch` + `response.body` 的 async iterator 读流。**必须处理跨 chunk 的半截帧** ——
网络分片不会正好落在帧边界上,维护一个 buffer、只在遇到 `\n\n` 时才切帧。
这一条是 Node 示例最容易写错、也最难被"看起来能跑"发现的地方,代码注释里点明。

- [ ] **Step 2: Java(JDK 8 + `HttpURLConnection`,零依赖)**

用 `HttpURLConnection` + `BufferedReader`(**`InputStreamReader` 必须显式指定 UTF-8**,
JDK 8 的默认字符集跟平台走,中文会乱码)。同样处理:空行分帧、跳 `:` 心跳、取 `id:` 里的 seq。
`setDoOutput(true)` + `getOutputStream()` 写 JSON body;JSON 用手拼字符串即可
(**不引 Gson/Jackson**,示例要零依赖),但要说明"生产代码请用 JSON 库,别手拼"。

- [ ] **Step 3: 本地编译检查**

```bash
mkdir -p /tmp/p3-java && python3 - <<'PY'
import pathlib, re
md = pathlib.Path("apps/admin-ui/docs-site/guide/examples.md").read_text()
for i, b in enumerate(re.findall(r"```java\n(.*?)```", md, re.S)):
    m = re.search(r"class\s+(\w+)", b)
    pathlib.Path(f"/tmp/p3-java/{m.group(1)}.java").write_text(b)
    print("wrote", m.group(1))
PY
javac -d /tmp/p3-java /tmp/p3-java/*.java && echo "javac OK"
node --check <(python3 -c "
import pathlib,re
md=pathlib.Path('apps/admin-ui/docs-site/guide/examples.md').read_text()
print(re.findall(r'\`\`\`js\n(.*?)\`\`\`', md, re.S)[0])
") && echo "node OK"
```
Expected: `javac OK`(JDK 8 下编译通过)+ 每个 js 块 `node --check` 通过。

- [ ] **Step 4: 构建 + Commit**

```bash
cd apps/admin-ui/docs-site && pnpm build
git add apps/admin-ui/docs-site/guide/examples.md
git commit -m "docs(external-api): 第 8 章多语言示例——Node.js + Java(JDK 8)"
```

### Task 6: 四语言逐条真机跑通(控制器自己做,不派实施者)

spec §十 的验收原话:「四语言示例**逐条真机跑通**(不是照抄能编译就算)」。

做法:`kubectl port-forward` 把测试集群的 control-plane 端口映到本机 → 在 pod 内用产品自己的
store 铸一把临时 `write` key → 四种语言逐个跑 8.1–8.7 → **无论成败都撤销 key 并验证它已失效**。
key 不写进任何文件,只在本次 shell 环境变量里。

判据取可观测的量,不是"没报错":

- 8.1:四种语言都收到 `end` 帧,且各自解析出的 `updates` 帧数**彼此相等**
- 8.5:带 `since_seq` 重连后,首帧 seq == `since_seq + 1`
- 8.6:取消后 `end.status == "interrupted"`
- 8.7:没有待审批 run 时,应当得到 `APPROVAL_NOT_FOUND` —— **能拿到这个错误码就说明请求形状对了**

跑出来的每一处不一致都回灌成 Task 5 的修复轮。

---

## 收尾

两个 PR 各自跑完全分支终审后开 PR;`.vitepress/dist/` 是 gitignored 的本地构建产物,
**不要提交**。合并顺序无所谓,后合的那个如果撞上 `config.mts` 就 rebase 一下。

---

### Task 7(PR-2 最后一个):清掉「信封」这个内部说法

**Files:**
- Modify: `apps/admin-ui/docs-site/guide/errors.md`(22 处)
- Modify: `apps/admin-ui/docs-site/guide/run-agent.md`(7 处)
- Modify: `apps/admin-ui/docs-site/guide/run-control.md`(5 处)
- Modify: `apps/admin-ui/docs-site/guide/conventions.md`(2 处)
- Modify: `apps/admin-ui/docs-site/guide/sse-events.md`(若前面几个 task 引入了这个词)

用户原话:「信封这个词在文档里很难理解」。这是**对外**文档,读者是第三方对接工程师,
「信封 / envelope」是我们内部对响应外壳的叫法,对他们没有任何提示作用。

**改的方向是尽量删掉这个词,而不是换一个词** —— 读者真正要判断的是
「这次能不能读到 `error.code`」,直接说这件事就够了,不需要术语。

- [ ] **Step 1: 表格与行内标注一律改成"有没有 `error.code`"**

```
| 403 | `FORBIDDEN`(裸 `detail` 形状,非统一信封) |
  → | 403 | `FORBIDDEN`(只有 `detail`,**没有 `error.code`**) |

| 403 | `AGENT_DISABLED`(统一信封,和 scope 无关) |
  → | 403 | `AGENT_DISABLED`(**能读到 `error.code`**,和 scope 无关) |

「这三条走统一信封(与上面三种"裸 detail"形状不同)」
  → 「这三条能读到 `error.code`(上面三种读不到)」
```

- [ ] **Step 2: 只在两个"需要一个名字"的地方保留称呼**

  1. `conventions.md` §2.4(定义处)—— 叫**「标准格式」**,并把 `{success, data, error}` 的
     JSON 直接摆在旁边;
  2. `errors.md` 开头「错误响应的形状不统一」那节 —— 两种形状分别叫
     **「标准格式」**(有 `error.code`)和**「简易格式」**(只有 `detail`),各给一段 JSON。

  其余所有位置改用 Step 1 的说法,或直接写 `{success, data, error}` 这个形状本身。

- [ ] **Step 3: 顺带把实现细节名从对外文档里去掉**

  `run-agent.md` / `errors.md` 里出现的「FastAPI 默认的 `detail` 形状」——
  对接方不需要知道我们用什么框架,改成「只有一个 `detail` 字段的简易格式」。

- [ ] **Step 3.5: 顺带统一 H1 的章节号**(既有不一致,#1167 留下的)

现状:`auth.md` / `best-practices.md` / `conventions.md` 的 H1 带章节号(`# 3 认证`),
而 `quickstart.md` / `run-agent.md` / `errors.md` / `sse-events.md` 不带。侧栏是带号的,
两边对不上。**统一成带号**,照侧栏的文字:

```
# 1 概述与对接流程   # 4 接口详情   # 5 SSE 事件格式   # 6 错误码总表   # 8 附录:多语言示例
```

`run-control.md` 的 `# 4.2 / 4.7 …` 保持原样(它本来就是两节的合页)。
改 H1 不影响侧栏链接(侧栏指的是 `.md` 路径与二级锚点,不是 H1)。

- [ ] **Step 4: 验证**

```bash
rg -n "信封|envelope|FastAPI" apps/admin-ui/docs-site/guide/
```
Expected: **零命中**。然后:

```bash
cd apps/admin-ui/docs-site && pnpm build
```
Expected: 构建成功(锚点改动会体现在这里 —— 若改到了标题,侧栏链接要同步更新)。

**别改语义**:哪些码走哪种形状是既有事实,这个 task 只动措辞。改完逐条对读一遍,
确认没把「有 `error.code`」和「没有」写反 —— 写反比用术语坏得多。

- [ ] **Step 5: Commit**

```bash
git add apps/admin-ui/docs-site
git commit -m "docs(external-api): 去掉对外文档里的「信封」内部说法,改说有没有 error.code"
```
