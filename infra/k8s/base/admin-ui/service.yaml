apiVersion: v1
kind: Service
metadata:
  name: admin-ui
  labels:
    app.kubernetes.io/name: admin-ui
spec:
  selector:
    app.kubernetes.io/name: admin-ui
  ports:
    - name: http
      port: 8080
      targetPort: http
