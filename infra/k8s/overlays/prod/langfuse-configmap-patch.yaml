# Production values for base/langfuse/config.yaml's neutral blanks —
# 2026-09-07 填入真值. Mirrors
# overlays/test/langfuse-configmap-patch.yaml(同一平台 bucket 的
# `langfuse/` 前缀,decision 2026-07-31)。
apiVersion: v1
kind: ConfigMap
metadata:
  name: langfuse-config
data:
  NEXTAUTH_URL: "https://langfuse.deepaihealth.com"
  LANGFUSE_S3_EVENT_UPLOAD_BUCKET: "expert-work-prod"
  LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT: "https://s3.oss-cn-hangzhou-internal.aliyuncs.com"
  LANGFUSE_S3_EVENT_UPLOAD_REGION: "cn-hangzhou"
